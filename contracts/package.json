{
  "name": "contracts",
  "version": "1.0.0",
  "description": "CarShare Escrow Smart Contract",
  "type": "module",
  "scripts": {
    "node": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-escrow.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy-escrow.js --network sepolia",
    "test": "hardhat test"
  },
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.6",
    "dotenv": "^16.4.7",
    "ethers": "^6.13.5",
    "hardhat": "^3.2.0"
  }
}
